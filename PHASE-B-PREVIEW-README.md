# Phase B Preview

This is a safe parallel preview. It does not modify `index.html` or the current journeys.

## Upload

Copy `phase-b-preview.html` and `engine/browser/` into the repository root.

## Test URL

`https://lsbud-john.github.io/Safe-Digging-Hub-V3/phase-b-preview.html`

## What it tests

- Browser loading of published runtime JSON
- Matching free text against all runtime intents
- Clear, ambiguous and no-match states
- User confirmation before journey state begins
- Initial matrix-linked IDs in a developer panel
- The next question from `runtime/question-rules.json`

The live `index.html` remains unchanged.
