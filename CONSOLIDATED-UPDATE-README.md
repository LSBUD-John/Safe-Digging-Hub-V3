# Consolidated Phase B Preview Update

This package supersedes the earlier individual preview patches.

## Upload

Upload all files preserving paths. Replace the active `package.json`. The matrix workflow will generate runtime v3.

## Verify

`runtime/manifest.json` should show version 3, 33 controlled relations, 9 intent families, and flowModel `understand-plan-locate-excavate-safely`.

## Preview

Open `/phase-b-preview.html`. The live `/index.html` remains unchanged.
